import 'package:flutter/material.dart';

class ReviewsTabPage extends StatelessWidget {
  const ReviewsTabPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("ReviewsTabPage"),
    );
  }
}